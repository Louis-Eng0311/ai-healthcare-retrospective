services:
  # 1. Redis (메시지 브로커 및 캐싱)
  redis:
    image: redis:alpine
    networks:
      - ws
    ports:
      - "${REDIS_PORT:-6379}:6379"
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s

  # 2. Database
  mysql:
    image: mysql:8.0
    restart: always
    environment:
    # ${환경변수명} 문법은 OS 환경변수 혹은 .env에서 해당 환경변수명과 일치하는 항목이 있으면 대입합니다.
    # 반드시 사전에 .env에 해당 항목이 존재하는지 확인해야 합니다.
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-rootpw1234}
      MYSQL_DATABASE: ${DB_NAME:-ai_health}
      MYSQL_USER: ${DB_USER:-ozcoding}
      MYSQL_PASSWORD: ${DB_PASSWORD:-pw1234}
      TZ: 'Asia/Seoul'
    ports:
      - ${DB_EXPOSE_PORT:-3306}:${DB_PORT:-3306}
    networks:
      - ws
    volumes:
      - mysql_data:/var/lib/mysql  # 컨테이너가 삭제돼도 데이터는 호스트 파일시스템에 남음
      - ./docker/mysql-init:/docker-entrypoint-initdb.d:ro
    command: # 한글 저장을 위한 문자셋 설정 (필수!)
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
      - --port=${DB_PORT:-3306}
    healthcheck:
      test: ["CMD", "mysqladmin" ,"ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  # 3. API Server (FastAPI)
  fastapi:
    build:
      context: .  # 프로젝트 루트를 빌드 컨텍스트로 지정
      dockerfile: app/Dockerfile  # Dockerfile 위치 지정
    image: ${DOCKER_USER:-local}/${DOCKER_REPOSITORY:-ai-health}:app-${APP_VERSION:-v1.0.0}  # APP_VERSION은 빌드된 이미지의 버전관리를 위함입니다. v1.0.0 형식으로 사용합니다.
    env_file: .env
    environment:
      REDIS_URL: redis://redis:6379/0
      CHAT_WORKER_QUEUE: chat_tasks
    command: | 
      sh -c "uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"
    volumes:
      - ./app:/app/app  # 로컬의 app 디렉토리를 컨테이너의 /app/app에 마운트(수정사항 즉시 반영)
      - ./prompts:/app/prompts
      - static_volume:/app/templates
      - uploads_data:/app/uploads
    restart: always
    ports:
      - "${APP_PORT:-8000}:8000"
    networks:
      - ws
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy

  # 4. AI Worker (Model 학습 및 추론)
  ai-worker:
    build:
      context: .  # 프로젝트 루트를 빌드 컨텍스트로 지정
      dockerfile: ai_worker/Dockerfile  # Dockerfile 위치 지정
    image: ${DOCKER_USER:-local}/${DOCKER_REPOSITORY:-ai-health}:ai-${AI_WORKER_VERSION:-v1.0.0}  # AI_WORKER_VERSION은 빌드된 이미지의 버전관리를 위함입니다. V1.0.0 형식으로 사용합니다.
    env_file: .env
    environment:
      DB_HOST: mysql
      DB_PORT: 3306
      REDIS_URL: redis://redis:6379/0
      AI_WORKER_QUEUE: ai_tasks
      CHAT_WORKER_QUEUE: chat_tasks
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      OPENAI_MODEL: ${OPENAI_MODEL}
      MFDS_SERVICE_KEY: ${MFDS_SERVICE_KEY}
      KIDS_API_KEY: ${KIDS_API_KEY}
    restart: on-failure
    mem_limit: 4G
    networks:
      - ws
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy

  # 5. Nginx (웹 서버 - 요청 리버스 프록시로 전달)
  nginx:
    image: nginx:latest
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
      - static_volume:/vol/web/static
      - ./app/ui/auth-demo:/var/www/auth-demo:ro
    ports:
      - "${NGINX_PORT:-80}:80"
    networks:
      - ws
    depends_on:
      - fastapi

  frontend:
    image: node:20-alpine
    working_dir: /frontend
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0 --port 5173"
    environment:
      VITE_API_PROXY: http://nginx
    volumes:
      - ./frontend:/frontend
    ports:
      - "5173:5173"
    networks:
      - ws
    depends_on:
      - fastapi

    # app_worker:
  app-worker:
    container_name: app-worker
    build:
      context: .
      dockerfile: app/Dockerfile
    env_file: .env
    command: sh -c "uv run python -u -m app_worker.main"
    restart: unless-stopped
    networks:
      - ws
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./app:/app/app
      - ./app_worker:/app/app_worker
      - uploads_data:/app/uploads

volumes:
  mysql_data:
  static_volume:
  uploads_data:

networks:
  ws:
    driver: bridge
